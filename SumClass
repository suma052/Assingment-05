// 1️⃣ SumClass
class SumClass {
    static double sumSeries() {
        double i = 1.0, sum = 0.0;
        do {
            sum += i;
            i -= 0.1;
        } while (i >= 0.1);
        return Math.round(sum * 10.0) / 10.0; // rounding
    }
}
